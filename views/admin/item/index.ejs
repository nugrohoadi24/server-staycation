
<%- include('../../components/header.ejs') %>

<!-- Page Wrapper -->
<div id="wrapper">

  <!-- Sidebar -->
  <%- include('../../components/sidebar.ejs') %>
  <!-- End of Sidebar -->

  <!-- Content Wrapper -->
  <div id="content-wrapper" class="d-flex flex-column">

    <!-- Main Content -->
    <div id="content">

      <!-- Topbar -->
      <%- include('../../components/navbar.ejs') %>
      <!-- End of Topbar -->

      <!-- Begin Page Content -->
      <div class="container-fluid">

        <% if(action == 'view') { %> 
          <!-- Page Heading -->
          <h1 class="h3 mb-4 text-gray-800">Item</h1>

          <%- include('./table.ejs') %>
          <%- include('./modal/add_modal.ejs') %>
          <%- include('./modal/edit_modal.ejs')  %> 
        <% } else if (action == 'image') { %>
          <%- include('./view_detail.ejs') %>
        <% } %> 
      </div>
      <!-- /.container-fluid -->

    </div>
    <!-- End of Main Content -->

    <!-- Footer -->
    <%- include('../../components/footer.ejs') %>
    <!-- End of Footer -->

  </div>
  <!-- End of Content Wrapper -->

</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
      <div class="modal-footer">
        <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
        <a class="btn btn-primary" href="/logout">Logout</a>
      </div>
    </div>
  </div>
</div>

  <%- include('../../components/script.ejs') %>

</body>

<script>
  $('#dataTable').on('click', '.btn-edit', function() {
    let id = $(this).data('id');
    let title = $(this).data('title');
    let price = $(this).data('price');
    let city = $(this).data('city');
    let description = $(this).data('description');

    $('#edit_modal').modal('show');
    $('#id').val(id);
    $('.title').val(title);
    $('.price').val(price);
    $('.city').val(city);
    $('.description').val(description);
  })

  $('#dataTableFeature').on('click', '.btn-edit-feature', function() {
    let id = $(this).data('id');
    let name = $(this).data('name');
    let qty = $(this).data('qty');

    $('#edit_modal_feature').modal('show');
    $('#id').val(id);
    $('.name_feature').val(name);
    $('.qty').val(qty);
  })

  $('#dataTableActivity').on('click', '.btn-edit-activity', function() {
    let id = $(this).data('id');
    let name = $(this).data('name');
    let type = $(this).data('type');

    $('#edit_modal_activity').modal('show');
    $('#id').val(id);
    $('.name_activity').val(name);
    $('.type').val(type);
  })
</script>

</html>